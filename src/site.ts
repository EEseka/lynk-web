// latest/download always points at the newest release, so every release must name its APK lynk.apk.
export const APK_URL = 'https://github.com/EEseka/Lynk/releases/latest/download/lynk.apk';

export const APP_STORE_URL: string | null = null;
export const GOOGLE_PLAY_URL: string | null = null;

export const SUPPORT_EMAIL = 'support@lynk.com.ng';
